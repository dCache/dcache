Source distribution + javadoc of CoG JGlobus 1.4
http://dev.globus.org/wiki/CoG_JGlobus_1.4

note:

The Cog Library depends (among others) on a modified version of puretls v0.9b4. The distribution contains only the puretls binary, not the modified sources.


how to build the jars:

 - unzip cog-jglobus-1.4-src.tar.gz
 - cd cog-jglobus-1.4
 - copy 3rd party jars needed for compilation only
 		cp ../lib/*.jar lib/
 		
 - apply the patches: 
 
 		../../cog-srmlib/cog-jglobus-1.4*.patch
 		
 - build the cog distro
 
 		ant dist
 		
 		(result should be in build/cog-jglobus-1.4/ )
