package org.dcache.cells;

/**
 * Classes implementing this object are registered as Cell command
 * listener. Command listeners are scanned for methods with the prefix
 * ac_. Such methods are exposed to the user via a command shell.
 */
public interface CellCommandListener
{

}
