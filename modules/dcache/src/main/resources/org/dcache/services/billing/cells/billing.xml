<?xml version="1.0" encoding="UTF-8"?>

<!--+
    |  The billing components that are common between dCache instances that make
    |  use of database storage and those that don't.
    +-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">

  <bean id="properties"
    class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    <description>Imported configuration data</description>
    <property name="location" value="arguments:"/>
  </bean>

  <bean id="poolmanager-stub" class="org.dcache.cells.CellStub">
    <description>Pool manager communication stub</description>
    <property name="destination" value="${dcache.service.poolmanager}"/>
    <property name="timeout" value="${poolConnectTimeout}"/>
  </bean>

  <bean id="billingcell"
    class="org.dcache.services.billing.cells.BillingCell">
    <property name="logsDir" value="${billingLogsDir}"/>
    <property name="disableTxt" value="${billingDisableTxt}"/>
    <property name="printMode" value="${printMode}"/>
    <property name="poolManagerStub" ref="poolmanager-stub"/>
  </bean>

  <!-- Set of beans instantiated if database is enabled -->
  <beans profile="db-yes">
    <bean id="jdbc-billing-info-access" class="${billingInfoAccess}"
          init-method="initialize" destroy-method="close">
      <property name="jdbcUrl" value="${db.url}"/>
      <property name="jdbcDriver" value="${db.driver}"/>
      <property name="jdbcUser" value="${db.user}"/>
      <property name="jdbcPassword"
        value="#{ T(diskCacheV111.util.Pgpass).getPassword('${db.password.file}', '${db.url}', '${db.user}', '${db.password}') }"/>
      <property name="maxInsertsBeforeCommit"
        value="${billingMaxInsertsBeforeCommit}"/>
      <property name="maxTimeBeforeCommit"
        value="${billingMaxTimeBeforeCommitInSecs}"/>
      <property name="propertiesPath" value="${billingInfoAccessPropertiesFile}"/>
      <property name="partitionCount" value="${db.connections.partition-count}"/>
      <property name="maxConnectionsPerPartition" value="${db.connections.max-per-partition}"/>
      <property name="minConnectionsPerPartition" value="${db.connections.min-per-partition}"/>
    </bean>

    <bean id="histogram-service" class="org.dcache.services.billing.histograms.data.JDOTimeFrameHistogramDataService">
      <property name="access" ref="jdbc-billing-info-access"/>
    </bean>

    <bean id="histogram-request-receiver" class="org.dcache.services.billing.cells.receivers.HistogramRequestReceiver">
      <property name="service" ref="histogram-service"/>
    </bean>

    <bean id="billing-info-message-receiver" class="org.dcache.services.billing.cells.receivers.BillingInfoMessageReceiver">
      <property name="access" ref="jdbc-billing-info-access"/>
    </bean>

    <bean id="liquibase" class="org.dcache.util.SpringLiquibase">
      <description>Database schema manager</description>
      <property name="dataSource">
        <bean class="org.springframework.jdbc.datasource.DriverManagerDataSource">
          <property name="driverClassName" value="${db.driver}"/>
          <property name="url" value="${db.url}"/>
          <property name="username" value="${db.user}"/>
          <property name="password"
                    value="#{ T(diskCacheV111.util.Pgpass).getPassword('${db.password.file}', '${db.url}', '${db.user}', '${db.password}') }"/>
        </bean>
      </property>
      <property name="changeLog" value="classpath:${db.schema.changelog}"/>
      <property name="shouldUpdate" value="${db.schema.auto}"/>
    </bean>
  </beans>
</beans>
