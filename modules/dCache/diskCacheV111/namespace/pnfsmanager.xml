<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-2.5.xsd">

  <bean id="properties" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    <description>Imported configuration data</description>
    <property name="location" value="arguments:"/>
  </bean>

  <bean id="pnfs-manager" class="diskCacheV111.namespace.PnfsManagerV3"
        init-method="init">
      <description>Request processor</description>
      <property name="threads" value="${threads}"/>
      <property name="threadGroups" value="${threadGroups}"/>
      <property name="listThreads" value="${listThreads}"/>
      <property name="cacheLocationThreads" value="${cachelocation-threads}"/>
      <property name="cacheModificationRelay" value="${cmRelay}"/>
      <property name="pnfsDeleteNotificationRelay" value="${pnfsDeleteRelay}"/>
      <property name="logSlowThreshold" value="${logSlowThreshold}"/>
      <property name="folding" value="${folding}"/>
      <property name="directoryListLimit" value="${directoryListLimit}"/>
      <property name="permissionHandler" ref="permission-handler"/>
      <property name="nameSpaceProvider">
          <bean class="diskCacheV111.namespace.provider.PermissionHandlerNameSpaceProvider">
              <constructor-arg ref="name-space-provider"/>
              <constructor-arg ref="permission-handler"/>
          </bean>
      </property>
      <property name="cacheLocationProvider" ref="cache-location-provider"/>
      <property name="queueMaxSize" value="${queueMaxSize}"/>
  </bean>

  <bean id="name-space-provider" class="${namespace-provider}"
        factory-method="getProvider">
      <description>Name space provider</description>
      <constructor-arg value="${arguments}"/>
  </bean>

  <bean id="cache-location-provider" class="${cachelocation-provider}"
        factory-method="getProvider">
      <description>Cache location provider</description>
      <constructor-arg value="${arguments}"/>
  </bean>

  <bean id="permission-handler"
        class="org.dcache.namespace.ChainedPermissionHandler">
      <description>Permission handler</description>
      <property name="chain">
          <list>
              <bean class="org.dcache.namespace.ACLPermissionHandler"/>
              <bean class="org.dcache.namespace.PosixPermissionHandler"/>
          </list>
      </property>
  </bean>

  <bean id="acl-handler"
        class="org.dcache.acl.handler.singleton.AclHandler"
        factory-method="instance">
      <description>ACL handler</description>
      <property name="properties">
          <props>
              <prop key="aclEnabled">${aclEnabled}</prop>
              <prop key="aclTable">${aclTable}</prop>
              <prop key="aclConnDriver">${aclConnDriver}</prop>
              <prop key="aclConnUrl">${aclConnUrl}</prop>
              <prop key="aclConnUser">${aclConnUser}</prop>
              <prop key="aclConnPswd">${aclConnPswd}</prop>
          </props>
      </property>
  </bean>
</beans>
