#!/bin/sh

if [ -z "$SRM_PATH" ]; then
    SRM_PATH="/usr/share/srm"
fi

if [ ! -d "$SRM_PATH" ]; then
    echo "${SRM_PATH} is not a directory"
    exit 2
fi

export SRM_PATH

/usr/sbin/srm -advisoryDelete $*
