<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.dcache</groupId>
  <artifactId>logback-test-config</artifactId>
  <version>2.12.41</version>
  <packaging>jar</packaging>

  <name>Logback config for building and testing</name>
  <url>http://www.dcache.org</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <prerequisites>
      <maven>3.0.4</maven>
  </prerequisites>

  <distributionManagement>
      <downloadUrl>https://download.dcache.org/nexus/content/repositories/releases/</downloadUrl>

      <repository>
          <uniqueVersion>false</uniqueVersion>
          <id>dcache.release.repository</id>
          <name>dCache.org release repository</name>
          <url>https://download.dcache.org/nexus/content/repositories/releases/</url>
          <layout>default</layout>
      </repository>
      <snapshotRepository>
          <uniqueVersion>true</uniqueVersion>
          <id>dcache.snapshot.repository</id>
          <name>dCache.org snapshot repository</name>
          <url>https://download.dcache.org/nexus/content/repositories/snapshots/</url>
          <layout>default</layout>
      </snapshotRepository>
  </distributionManagement>

  <dependencies>
      <dependency>
          <groupId>ch.qos.logback</groupId>
          <artifactId>logback-classic</artifactId>
          <!-- There are two issues with 1.1.1:
               http://jira.qos.ch/browse/LOGBACK-959
               http://jira.qos.ch/browse/LOGBACK-961;
               while we have put a workaround for the first
               in place, the second still affects
               dcache alarm send (it hangs after sending) -->
          <version>1.0.12</version>
      </dependency>
  </dependencies>

  <build>
      <plugins>
          <!-- Needed to allow animal-sniffer to run from the command-line -->
          <plugin>
              <groupId>org.codehaus.mojo</groupId>
              <artifactId>animal-sniffer-maven-plugin</artifactId>
              <version>1.7</version>
              <configuration>
                  <signature>
                      <groupId>org.codehaus.mojo.signature</groupId>
                      <artifactId>java16-sun</artifactId>
                      <version>1.0</version>
                  </signature>
              </configuration>
          </plugin>
      </plugins>
  </build>

</project>
