# Load arguments into environment

check -strong statisticsLocation

create diskCacheV111.services.PoolStatisticsV0 ${cell.name}  \
     "${statisticsLocation}  \
#      -create  \
#      -htmlBase=${statisticsLocation}  \
      -domain=${thisFqHostname}"

