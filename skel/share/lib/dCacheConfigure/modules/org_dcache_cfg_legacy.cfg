[org_dcache_gplasma]

description = "Sets up gPlasma to use the output of dCachevoms2gplasma to provide authorisation."

# supported languages are python and bash in a scientific linux compatable OS
language = "bash"
# files are parsed in order searching in each directory
path_files = central_certs,
    cron_job,
    set_mysql_passwd,
    users_getfirstpoolaccount,
    users_getspecialusers,
    yaimlog,
    check_users_conf_format,
    requires,
    split_quoted_variable,
    users_getprduser,
    users_getvogroup,
    config_file,
    run,
    userhandling,
    users_getsgmuser,
    users_getvoprefix,
    config_pgsql,
    config_sedcache,
    config_sechimera

# directories are parsed in order
path_directories = /opt/d-cache/share/dCacheConfigure/modules/,
    /opt/d-cache/share/dCacheConfigure/utils/

# List of variables required to use the function.
requirements = []

# the function/method to execute in bash/python to configure
run = config_dcache_vorolemap_authz_update

# The function to check the module should be run.
# bash modules returns '0' if they should be run.
# python modules returns 'True' if they should be run.
check = config_sechimera_check


[org_dcache_cron]

description = "Sets up cron jobs necessary for good operation of dCache"

# supported languages are python and bash in a scientific linux compatable OS
language = "bash"
# files are parsed in order searching in each directory
path_files = central_certs,
    cron_job,
    set_mysql_passwd,
    users_getfirstpoolaccount,
    users_getspecialusers,
    yaimlog,
    check_users_conf_format,
    requires,
    split_quoted_variable,
    users_getprduser,
    users_getvogroup,
    config_file,
    run,
    userhandling,
    users_getsgmuser,
    users_getvoprefix,
    config_pgsql,
    config_sedcache,
    config_sepnfs,
    config_sechimera

# directories are parsed in order
path_directories = /opt/d-cache/share/dCacheConfigure/modules/,
    /opt/d-cache/share/dCacheConfigure/utils/

# List of variables required to use the function.
requirements = []

# the function/method to execute in bash/python to configure
run = config_sedcache_cron

# The function to check the module should be run.
# bash modules returns '0' if they should be run.
# python modules returns 'True' if they should be run.
check = config_sechimera_check


[org_dcache_cfg_gplasma_policy]

description = "Sets up gPlazma to use the output of dCachevoms2gplasma to provide authorisation."

# supported languages are python and bash in a scientific linux compatable OS
language = "bash"
# files are parsed in order searching in each directory
path_files = central_certs,
    cron_job,
    set_mysql_passwd,
    users_getfirstpoolaccount,
    users_getspecialusers,
    yaimlog,
    check_users_conf_format,
    requires,
    split_quoted_variable,
    users_getprduser,
    users_getvogroup,
    config_file,
    run,
    userhandling,
    users_getsgmuser,
    users_getvoprefix,
    config_pgsql,
    config_sedcache,
    config_sepnfs,
    config_sechimera

# directories are parsed in order
path_directories = /opt/d-cache/share/dCacheConfigure/modules/,
    /opt/d-cache/share/dCacheConfigure/utils/

# List of variables required to use the function.
requirements = []

# the function/method to execute in bash/python to configure
run = yaim_config_dcache_files_key_value_srm_gplazma_policy

# The function to check the module should be run.
# bash modules returns '0' if they should be run.
# python modules returns 'True' if they should be run.
check = config_sechimera_check
