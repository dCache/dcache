CREATE TABLE AUTHGROUPLIST (id BIGINT NOT NULL, ATTRIBUTE VARCHAR(255), authrecord_id BIGINT, PRIMARY KEY (id))
CREATE TABLE AUTHGROUP (id BIGINT NOT NULL, gid INTEGER, name VARCHAR(255), grouplist_id BIGINT, PRIMARY KEY (id))
CREATE TABLE AUTHRECORD (id BIGINT NOT NULL, root VARCHAR(255), name VARCHAR(255), home VARCHAR(255), priority INTEGER, identity VARCHAR(255), uid INTEGER, read_only BOOLEAN, PRIMARY KEY (id))
ALTER TABLE AUTHGROUPLIST ADD CONSTRAINT FK_AUTHGROUPLIST_authrecord_id FOREIGN KEY (authrecord_id) REFERENCES AUTHRECORD (id)
ALTER TABLE AUTHGROUP ADD CONSTRAINT FK_AUTHGROUP_grouplist_id FOREIGN KEY (grouplist_id) REFERENCES AUTHGROUPLIST (id)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
